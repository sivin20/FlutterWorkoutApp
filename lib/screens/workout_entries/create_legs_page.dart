import 'package:flutter/material.dart';

class CreateLegsEntryPage extends StatelessWidget {
  const CreateLegsEntryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
