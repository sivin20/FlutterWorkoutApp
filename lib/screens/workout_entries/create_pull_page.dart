import 'package:flutter/material.dart';

class CreatePullEntryPage extends StatelessWidget {
  const CreatePullEntryPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Create new Pull entry')),
    );
  }
}
